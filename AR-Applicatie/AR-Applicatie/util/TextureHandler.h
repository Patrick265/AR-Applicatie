#pragma once


class TextureHandler {

private:

public:

	/*
		Adds a texture to the 

		@param image_path - The relative path of the image

		Returns the id of the texture
	*/
	static uint16_t addTexture(const std::string image_path);

};